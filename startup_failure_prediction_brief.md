# 初创企业失败预测系统

**项目说明书 · 2026**

数据来源：[loot-drop.io/rebuilds](https://www.loot-drop.io/rebuilds) · [loot-drop.io/database-view](https://www.loot-drop.io/database-view)

---

## 01 · 项目背景

全球每年有大量初创公司关闭，但这些失败案例留下了宝贵的信息，包括公司描述、融资规模、所在行业、运营时长以及最终失败的原因。

我们的数据来源是 **loot-drop.io**，一个收录了超过 1,670 家失败初创公司案例的数据库，每条记录包含结构化字段和文字描述。我们将以此为基础，训练一个能够在公司早期就判断其失败风险的预测模型。

> **核心价值：** 给定一家公司的基本信息和描述，模型能够输出一个失败风险概率，并说明主要风险点在哪里。

---

## 02 · 数据层

### 我们用什么数据？

数据分为两类，最终会融合在一起输入模型。

**结构化数据**
行业类别、产品类型、所在国家、融资总额、融资轮次、成立年份、运营时长、市场潜力评分、可扩展性评分等字段。

**文字描述数据**
公司简介、失败原因的详细描述、创始人陈述等非结构化文本，由语言模型处理成可计算的向量。

> **重要前提：** loot-drop 全部是失败案例。模型需要对比学习，因此还需要额外补充一批成功公司的数据（如 Crunchbase 上成功上市或持续经营的公司）作为对照组，否则模型没有比较基准。

---

## 03 · 模型层

### 模型如何理解一家公司？

模型由两条处理通道组成，最终汇聚输出一个预测结果。

**通道 A · 结构化特征**
行业、融资、时间等字段经过标准化处理后，直接输入分类器。
涵盖字段：`行业类别` `融资规模` `运营时长` `市场评分` `产品类型`

**通道 B · 文字理解**
公司描述和失败原因文字，通过语言模型（Sentence Transformer 或 OpenAI Embedding）转化为数百维的语义向量，捕捉单纯字段无法表达的信息，比如商业逻辑是否清晰、失败原因的模式等。
涵盖内容：`公司简介` `失败原因描述` `创始人陈述`

> 两条通道的输出拼接后，统一输入分类器，最终给出 0 到 1 之间的失败概率分数。

---

## 04 · 开发阶段

### 分四个阶段推进

**阶段 1 · 数据采集与清洗**
通过无头浏览器或 API 拦截方式从 loot-drop.io 获取全量失败案例，同时从 Crunchbase 等来源补充成功案例，清洗缺失值和格式不统一的字段。

**阶段 2 · 特征工程**
对结构化字段做编码和归一化，对文字描述批量生成语义向量，将两类特征拼接成统一的训练数据格式。

**阶段 3 · 模型训练与评估**
从简单的逻辑回归基线开始，逐步测试梯度提升树和轻量神经网络，使用 ROC-AUC 和准确率评估效果，选出最优方案。

**阶段 4 · 接口封装与交付**
将训练好的模型封装为 API，输入公司信息即可返回失败概率和主要风险因素，方便后续集成到任何前端产品中。

---

## 05 · 关键风险与注意事项

**样本不平衡**
数据集全是失败案例，必须引入成功案例做平衡，否则模型无意义。

**时间穿越**
只能用公司早期能获取到的信息作为输入特征，不能用后期才出现的数据。

**数据动态加载**
网站数据由 JavaScript 动态加载，需要浏览器自动化工具或拦截 API 才能获取，直接爬取 HTML 无效。

---

## 06 · 交付物

| 交付物 | 说明 |
|---|---|
| 清洗后的数据集（CSV） | 结构化 + 文字描述，标注成功/失败标签 |
| 特征工程脚本 | 包含编码、归一化、向量化全流程 |
| 训练好的模型文件 | 可直接加载使用 |
| 预测 API 接口 | 输入公司信息，返回风险概率 |
| 评估报告 | ROC-AUC、准确率、特征重要性分析 |

---

*数据来源：[loot-drop.io](https://www.loot-drop.io/rebuilds) · 本文件由 Claude (Anthropic) 生成 · 2026*
